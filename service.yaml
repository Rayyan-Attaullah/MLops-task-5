apiVersion: v1
kind: Service
metadata:
  name: mlops-task5-service
spec:
  type: NodePort
  selector:
    app: mlops-task5
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30007
